{
  "name": "http-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 3001"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
